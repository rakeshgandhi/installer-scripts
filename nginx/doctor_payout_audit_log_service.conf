upstream doctor_payout_audit_log_service {
    server 127.0.0.1:7268; 
    keepalive 32;
}

server {
    listen 8068;
    server_name _;

    client_max_body_size 50m;

    access_log /var/log/nginx/doctor_payout_audit_log_service.log;
    error_log  /var/log/nginx/doctor_payout_audit_log_service.log warn;

    location / {
        proxy_pass http://doctor_payout_audit_log_service;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_connect_timeout 60s;
        proxy_buffering off;
    }
}

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
